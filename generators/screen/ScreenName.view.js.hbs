import * as React from 'react';
import {View} from 'react-native';
import AppText from 'components/AppText';
import styles from './{{properCase name}}.styles';
// import {NAMESPACE} from './{{properCase name}}.constants';

function {{properCase name}}View() {
  return (
    <View style={styles.container}>
      <AppText text={'{{properCase name}}'}/>
    </View>
  );
}

export default React.memo({{properCase name}}View);
