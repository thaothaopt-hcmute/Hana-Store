export const getCategoriesSelector = (state) => state.idioms.categories;

export const getIdiomsSelector = (state) => state.idioms.idioms;

export const checkIsSelectedIdiomsSelector = (state) =>
  state.idioms.isSelectedIdioms;

// export const getFavoriteQuotesSelector = (state) => state.quotes.favoriteQuote;
